# First Agent — 环境变量模板
# 复制到 .env 后填写（.env 不提交到 git）
#
# 推荐配置入口：config/config.yaml 的 provider section
# .env 只放 secret（API key），不放 provider 类型/模型名
# 详见 README.md "Real Provider Opt-in" 章节

# --- Provider API keys ---
# 在 config/config.yaml 中设置 api_key_env 为以下变量名之一，
# 然后在 .env 中填写对应的 key 值：
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# --- Legacy 兼容（已废弃，不推荐）---
# 以下变量仅在 config/config.yaml 不存在时作为 fallback 生效
# MY_FIRST_AGENT_LLM_PROVIDER=fake
# MY_FIRST_AGENT_LLM_MODEL=
# MY_FIRST_AGENT_LLM_BASE_URL=
