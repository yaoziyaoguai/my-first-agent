# Example: Kimi K2.5 via Anthropic-compatible (DashScope)
# 使用方式：cp config/examples/kimi-anthropic-compatible.config.yaml config/config.yaml
# 然后将 api_key 替换为真实 key
#
# 推荐：使用 api_key_env 从环境变量读取 key（更安全）
#   1. 在 .env 中设置 ANTHROPIC_API_KEY=你的真实key
#   2. 将下方 api_key 行注释掉
#   3. 取消注释 api_key_env 行
#
# 用户只需配置 enabled/type/model/base_url 和 api_key 或 api_key_env。
# request_path 和 auth_scheme 由 adapter 内部决定，不需要用户配置。

provider:
  enabled: true
  type: anthropic_compatible
  model: kimi-k2.5
  base_url: https://coding.dashscope.aliyuncs.com/apps/anthropic
  api_key: sk-REPLACE_ME
  # api_key_env: ANTHROPIC_API_KEY  # 推荐：从环境变量读取 key
