# First Agent 统一项目配置
# 当前实际生效配置。如需切换到真实 LLM，复制 config/examples/ 下对应示例文件：
#   cp config/examples/kimi-anthropic-compatible.config.yaml config/config.yaml
# 或
#   cp config/examples/glm-openai-compatible.config.yaml config/config.yaml

provider:
  enabled: false
  type: fake
  model: fake-llm
