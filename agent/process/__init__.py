"""015 governed local action capability package.

高内聚的 process capability：admission 解析 command identity 与 secret-minimized
spawn profile，runner 消费已验证 intent 做一次 bounded POSIX lifecycle（U5）。该包不
拥有 Runtime state、不读取 checkpoint、不认识 Goal/approval/model。
"""
